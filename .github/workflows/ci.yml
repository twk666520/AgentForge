
name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          pip install hatchling ruff pytest Pillow
          pip install -e core/
      - name: Lint
        run: ruff check core/src/ skills/skill-ocr/src/ skills/skill-translate/src/ skills/skill-vision/src/ skills/skill-desktop/src/
      - name: Format
        run: ruff format --check core/src/ skills/skill-ocr/src/ skills/skill-translate/src/ skills/skill-vision/src/ skills/skill-desktop/src/
      - name: Test Core
        run: pytest core/tests/ -v
      - name: Test OCR
        run: PYTHONPATH=skills/skill-ocr/src pytest skills/skill-ocr/tests/ -v
      - name: Test Translate
        run: PYTHONPATH=skills/skill-translate/src pytest skills/skill-translate/tests/ -v
      - name: Test Vision
        run: PYTHONPATH=skills/skill-vision/src pytest skills/skill-vision/tests/ -v
      - name: Test Desktop
        run: PYTHONPATH=skills/skill-desktop/src pytest skills/skill-desktop/tests/ -v
